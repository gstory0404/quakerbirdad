name: quakerbirdad
description: Flutter信天翁广告插件
version: 0.0.1
homepage: https://github.com/gstory0404/quakerbirdad

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
#  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
#  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.gstory.quakerbirdad
        pluginClass: QuakerbirdadPlugin
      ios:
        pluginClass: QuakerbirdadPlugin

